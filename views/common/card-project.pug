include card
include button

//- Card mixin used on project pages.
//- Attributes: 
//-   name: Name of the project. Used as a title under the image.
//-   imgsrc: image URL to use for the top of the card.
//- Block: body of the card, generally text and links (see styled-links).
mixin card-project(name, imgsrc, imglink)
    +card(shape="w-72 h-[28rem] sm:w-80 rounded-lg", hover=false)
        div(class="rounded-lg overflow-clip")
            a(href=imglink) 
                img(src=imgsrc class="w-full h-32 sm:h-48 object-cover" alt="")
        div(class="text-white text-center pt-4 flex flex-col h-72 sm:h-56")
            h3(class="text-lg")
                =name
            block

//- Mixin to generate inside a project card.
//- Attributes:
//-  buttons: an array of objects, each with a `name` (text inside the button), `href` (link URL), and `icon` (Font Awesome class)
mixin project-buttons(buttons)
    div(class="flex justify-center flex-wrap")
        if buttons
            each button in buttons
                +button(button.href, "_blank")
                    i(class=button.icon)
                    span
                        ="  " + button.name
